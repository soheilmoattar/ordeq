[project]
name = "starter-package"
version = "0.1.0"
description = "Starter project for Ordeq using a package structure"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ordeq",
    "ordeq-requests",
    "ordeq-yaml"
]
