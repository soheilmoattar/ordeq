[build-system]
requires = ["setuptools>=75.0", "setuptools_scm>=8.1.0"]
build-backend = "setuptools.build_meta"

[project]
dependencies = ["ordeq", "ordeq-polars", "marimo"]
name = "ordeq-docs"
version = "0.1.0"

[tool.setuptools.packages.find]
namespaces = true
where = ["src"]
